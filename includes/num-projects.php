{exp:channel:entries dynamic="no" channel="submissions" limit="100" status="open|closed|Pending|Showcase|Archive" author_id="{embed:zoo_member_id}" }
{if count==1}{total_results}{/if}
{if no_results}0{/if}
{/exp:channel:entries}